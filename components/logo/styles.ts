import styled from 'styled-components'

const StyledLogo = styled.span`
  color: var(--primary);
  font-size: 24px;
  font-weight: 700;
  line-height: 16px;
  span {
    color: var(--secondary);
    font-size: 16px;
  }
`;

export { StyledLogo }